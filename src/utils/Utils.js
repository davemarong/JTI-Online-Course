// Functions
export const handleEmailInput = (event) => {
  setEmail(event.target.value);
};
export const handleUsernameInput = (event) => {
  setUsername(event.target.value);
};
export const handlePasswordInput = (event) => {
  setPassword(event.target.value);
};
